# Записується число
а = ціле(ввід("ВВедіть число: "))

# Умова та цикл
якщо а > 0:
    лічильник = 0
    поки лічильник <= а:
        друк("Привіт, Світ!")
        лічильник += 1
інакше :
    друк("Введіть додатне число")