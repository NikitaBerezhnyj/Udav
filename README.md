# Мова програмування Удав

<p align='center'>
  <img src='./VS Code Extintion/udav-extantion/icon-udav.png' alt='Udav icon' style="width:50%">
</p>

***Мова програмування Удав*** - це жартівлива мова програмування, створена на основі Python. Ідея виникла, коли мій друг, не програміст, назвав Python "Удавом". З тих пір у нас в компанії ходив прикол про "Удава".

Коли я вирішив створити щось більш менш серйозне, я вирішив провести маленьку підготовку та випробування своїх знань, написавши мову програмування Удав. Я намагався не напружуватися і просто отримати задоволення від процесу створення, оскільки це все ще лишається жартом.

За основу я взяв мову програмування Python, використовуючи матеріали, які я знайшов у [тут](https://github.com/davidcallanan/py-myopl-code).

***Як ви вже зрозуміли мова програмування називається Удав, тож логічно буде, якщо код цією мовою писався з застосуванням snake_case***

***Швидкий перехід між розділами:***

- [Мова програмування Удав](#мова-програмування-удав)
  - [Мова програмування Удав](#мова-програмування-удав-1)
    - [Інсталяція](#інсталяція)
      - [Ініціалізація](#ініціалізація)
      - [Деніціалізація](#деніціалізація)
    - [Синтаксис](#синтаксис)
    - [Приклад коду](#приклад-коду)
    - [Команди інтерпретатора](#команди-інтерпретатора)
  - [Власне IDE для Удав](#власне-ide-для-удав)
  - [Розширення для VS code](#розширення-для-vs-code)

___

## Мова програмування Удав

### Інсталяція

Щоб запустити інтерпретатор Удава, вам необхідно буде виконати один з наступних варіантів:

___1. Скомпілювати інтерпретатор___ (це дасть вам більше можливостей у майбутньому)

___2. Запустити інтерпретатор без компіляції___ (це також підійде, якщо ви просто хочете спробувати мову Удав і не хочете встановлювати або компілювати файли)

***1. Компіляція інтерпретатора***

Компілювання інтерпретатора необхідно для отримання виконуваного файлу udav. Для цього виконайте наступні кроки:

1. Клонуйте репозиторій з GitHub:

``` bash
git clone https://github.com/NikitaBerezhnyj/Udav.git
```

2. Перейдіть в теку з проектом:

``` bash
cd Udav
```

3. Встановіть пакет pyinstaller:

``` bash
pip install pyinstaller
```

4. Запустіть компіляцію:

``` bash
pyinstaller -F -n udav shell.py
```

Після завершення компіляції в теці з проектом буде створений виконуваний файл udav. Після ініціалізації цей файл можна буде використовувати для запуску програм мовою Удав з будь-якої теки.

***2. Запустити інтерпретатор без компіляції***

Щоб запустити інтерпретатор без компіляції, виконайте наступну команду в теці з проектом:

1. Клонуйте репозиторій з GitHub:

``` bash
git clone https://github.com/NikitaBerezhnyj/Udav.git
```

2. Перейдіть в теку з проектом:

``` bash
cd Udav
```

3. Запуск інтерпретатора

``` bash
python shell.py
```

Ця команда запустить інтерпретатор Удав в теці з проектом. Ви зможете написати і запустити програму, не компілюючи інтерпретатор.

#### Ініціалізація

Ініціалізація інтерпретатора Удав необхідна для того, щоб можна було використовувати його з будь-якої теки на вашому комп'ютері. Для цього виконайте наступні дії:
1. Перейдіть у теку із скомпільованим інтерпретатором

``` bash
cd dist
```

2. Запустіть файл інтерпретатора

``` bash
./udav
```

3. Використайте наступну команду:
   
``` python
ініціалізація
```

4. Завершіть роботу інтерпретатора:

``` python
вихід
```

Після ініціалізації інтерпретатора файл udav буде скопійовано в теку з іншими утилітами, що дасть можливість використовувати його для запуску програм мовою Удав з будь-якої теки.

#### Деніціалізація

Деініціалізація інтерпретатора Удав необхідна, якщо ви більше не плануєте використовувати його. Для цього виконайте наступні кроки:

1. Відкрийте інтерпретатор

``` bash
udav
```

2. Використайте наступну команду:
   
``` python
деініціалізація
```

3. Завершіть роботу інтерпретатора:

``` python
вихід
```

Це видалить Удав з теки з утилітами, але залишить його у теці з проєктом, тому ви зможете використовувати його і потім, якщо забажаєте

### Синтаксис

Я намагався зробити синтаксис Удав простим і зрозумілим для початківців, але при цьому потужним і гнучким для досвідчених розробників. Тож перейдімо до роботи з мовою.

***Створення коментарів***

``` python
# Це коментар, його інтерпретатор не бачить
```

***Вивід тексту на екран***

``` python
# Вивід звичайного тексту
друк("Привіт, Світ!")

# Вивід змінної
змінна а = 10
друк(а)
```

***Оголошення змінних***

``` python
# Цілочисельна змінна
змінна а = 10

# Дійсна змінна
змінна б = 10.5

# Рядкова змінна
змінна в = "Рядок"

# Булева змінна
змінна г = правда

```

***Ввід користувача***

``` python
# Записується рядок
змінна а = ввід()

# Записується число
змінна б = ввід_числа()
```

***Оголошення масивів***

``` python
# Оголошення масиву цілих чисел
змінна а = [0, 1]

# Оголошення масиву дійсних чисел
змінна б = [0.5, 1.001]

# Оголошення масиву рядків
змінна в = ["привіт", "світ"]

# Оголошення масиву булевих виразів
змінна г = [правда, правда, брехня]

```

***Умови if (якщо), else if (інакше_якщо) та else (інакше)***

``` python
# Умова якщо запис в один рядок
якщо а < 5 тоді; друк("<5")

# Умова якщо повний запис
якщо а < 5 тоді
    друк("<5")

# Умова якщо-інакше запис в один рядок
якщо а < 5 тоді; друк("<5") інакше друк(">8")

# Умова якщо-інакше повний запис
якщо а < 5 тоді
    друк("<5") 
інакше друк(">8")

# Умова якщо-інакше_якщо-інакше запис в один рядок
якщо а < 5 тоді; друк("<5") інакше_якщо а > 5 і а < 8 тоді; друк("<5 and >8") інакше друк(">8")

# Умова якщо-інакше_якщо-інакше повний запис
якщо а < 5 тоді 
    друк("<5") 
інакше_якщо а > 5 і а < 8 тоді 
    друк("<5 and >8") 
інакше друк(">8")
```

***Оголошення циклу while (поки)***

``` python
# Приклад циклу while (поки) у варіанті для одного рядка
поки л < 10 тоді змінна л = л + 1; друк(л)

# Приклад циклу while (поки) в розписаному варіанті
змінна л = 0
поки л < 10 тоді
    змінна л = л + 1
    друк(л)
кінець
```

***Оголошення циклу for (для_кожного)***

``` python
# Приклад циклу for (для_кожного) у варіанті для одного рядка
для_кожного л = 0 до 10 тоді друк(л)

# Приклад циклу for (для_кожного) в розписаному варіанті
для_кожного л = 0 до 10 тоді
    друк("Привіт, Світ!")
кінець
```

***Оголошення функції***

``` python
# Найпростіші функції, що можна записати в один рядок записуються так:
функція Привіт_світ() -> друк("Привіт, світ!")

# Функції, що не можна записати в один рядок записуються так:
функція Привіт_світ()
	змінна рази = ввід_числа()
	друк(рази)
	для_кожного i = 0 до рази тоді
		друк("Привіт, Світ!")
	кінець
кінець
```

### Приклад коду

Цей код фактично не виконує жодної корисної функції, але є прикладом використання усіх синтаксичних структур, що було висвітлені раніше.

``` python
функція Тест(разів)
    для_кожного р = 0 до разів тоді
        друк("Введіть будь ласка число від 1 до 10: ")
        змінна номер = ввід_числа()
        якщо номер <= 5 тоді
            для_кожного л = 0 до номер тоді
                друк("Ви виграли!")
            кінець
        інакше_якщо номер > 5 і номер < 8 тоді
            поки номер < 8 тоді
                змінна номер = номер + 1
                друк(номер)
            кінець
        інакше друк("Кінець гри")
    кінець
кінець

друк("Скільки разів ви хочете зіграти? ")
змінна разів = ввід_числа()
Тест(разів)
```

### Команди інтерпретатора

- ***допомога*** - показує всі доступні команди для терміналу інтерпретатора та базовий синтаксис мови

- ***запуск*** - запускає файл .udav (приклад використання: ***запуск("приклад.udav")***)

- ***цт*** - ця тека. Показує шлях до поточної теки

- ***лф*** - лист файлів. Показує вміст поточної теки

- ***зт*** - змінити теку. Змінює поточну теку

- ***сф*** - створити файл. Створює файл у поточній теці (приклад використання: ***сф приклад.txt***, якщо розширення не вказане, то воно буде за замовчуванням встановлене, як .udav)

- ***ст*** - створити теку. Створює теку у поточній (приклад використання: ***ст тестова_тека***)

- ***очистити*** - очищує термінал інтерпретатора

- ***вихід*** - закриває інтерпретатор

- ***версія*** - показує поточну версію Удав, що встановлена на вашому ПК

- ***УДАВ*** - виводить основну інформацію про стан Удава

___

***[Дивитись код мови](./Programming%20Language/)***

## Власне IDE для Удав

Поки що не готове

___

***[Дивитись код IDE](./Udav%20IDE/)***

## Розширення для VS code

Поки що не готове

___

***[Дивитись код розширення](./VS%20Code%20Extintion/udav-extantion/)***
